# Changelog

## 0.1.0 (Unreleased)

Initial release of the Tripswitch Python SDK.

- Synchronous client with automatic SSE-based flag updates
- Circuit breaker evaluation via `execute()`
- Metric ingestion via `ingest()`
- Admin client for managing projects, flags, and environments
